@layer app-base {
  @tailwind base;

  html,
  body {
    @apply h-screen bg-background;
  }
}

@layer app-components {
  @tailwind components;
}

@layer app-utilities {
  @tailwind utilities;
}

@layer app-variants {
  @tailwind variants;
}

@layer app-overrides {
}
